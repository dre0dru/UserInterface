ShowScreenButton? рантайм конверт строки в тип


using UnityEngine;

namespace ScreensService
{
    public interface IStackedScreensService<in TScreenConstraint>
        where TScreenConstraint : Component
    {
        // ScreenStack<TScreen> GetScreenStack<TScreen>()
            // where TScreen : TScreenConstraint;

    }
}

using UnityEngine;

namespace ScreensService
{
    public interface IQueuedScreensService<in TScreenConstraint>
        where TScreenConstraint : Component
    {
        // ScreenQueue<TScreen> GetScreenQueue<TScreen>()
            // where TScreen : TScreenConstraint;
    }
}


может те, что для сцены переименовать в Persistent...?

можно ведь объединять по скрин контейнеру

создать уже отдельный сервис с доп функционалом, который будет хранить скрины по ключу, которые вытягиваются из контейнеров
он же будет их удалять при ненадобности?

Source - содержит инстансы либо префабы